var m = 7;
var j = 5;

function addTwoVars(m,j){
    return m+j;
    
}
console.log(addTwoVars(m,j));

function subTwoVars(m,j){
    return m-j;
}
console.log(subTwoVars(m,j));

function timesTwoVars(m,j){
    return m*j;
}
console.log(timesTwoVars(m,j));

function divideTwoVars(m,j){
    return m/j;
}
console.log(divideTwoVars(m,j));

addTheVars = (m, j) => m + j;
    console.log(addTheVars(m,j));

subTheVars = (m, j) => m - j;
     console.log(subTheVars(m,j));

timesTheVars = (m, j) => m * j;
     console.log(timesTheVars(m,j));

divideTheVars = (m, j) => m / j;
     console.log(divideTheVars(m,j));



// multiplies the first input value by the second and returns it
// const multiplier = (item, multi) => item * multi;
// multiplier(4, 2); // returns 8
